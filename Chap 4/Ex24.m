clc
clear
num = input('Enter an integer: ');
switch true
    
    case (num < -2 || num > 4)
        f1(num)
            case (num >= 0 && num <= 2)
        f2(num)
            case (num < 0 && num >= -2)
        f3(num)
            otherwise
        f4(num)
        
end